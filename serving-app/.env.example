# =============================================================================
# DATABRICKS DIGITAL TWIN - BACKEND CONFIGURATION
# =============================================================================
# This file contains ONLY the environment variables that are actually used in the backend code
# Copy this file to .env and configure with your specific values

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Application port (used in config.py and server.py)
PORT=8080

# Flask secret key for session management (used in config.py and server.py)
SECRET_KEY=your-secret-key-change-in-production

# =============================================================================
# DATABRICKS CONFIGURATION
# =============================================================================
# Databricks workspace connection details (used in config.py)
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_TOKEN=dapixxxxxx

# SQL Warehouse configuration (used in config.py - provide either WAREHOUSE_ID or DATABRICKS_HTTP_PATH)
WAREHOUSE_ID=your-warehouse-id

# Database schema configuration (used in config.py with defaults)
DATABRICKS_CATALOG=main
DATABRICKS_SCHEMA=your_schema
DATABRICKS_TABLE=bronze

# =============================================================================
# ADVANCED TABLE CONFIGURATIONS
# =============================================================================
# Full table names for specialized data (used in config.py with auto-generated defaults)
SYNCED_TABLE_FULL_NAME=main.your_schema.synced_table
TRIPLE_TABLE_FULL_NAME=main.your_schema.latest_sensor_triples

# RDF models table configuration (used in services/rdf_models.py)
RDF_MODELS_FULL_TABLE_NAME=main.deba.rdf_models

# =============================================================================
# POSTGRESQL CONFIGURATION (LAKEBASE)
# =============================================================================
# PostgreSQL connection parameters (used in config.py)
PGDATABASE=main
PGUSER=your_username
PGPASSWORD=your-password
PGHOST=your-workspace.cloud.databricks.com
PGPORT=5432
PGSSLMODE=require
PGAPPNAME=databricks-digital-twin

# Lakebase instance configuration (used in config.py)
LAKEBASE_INSTANCE_NAME=your-instance-name

# Token refresh interval in seconds - default 900 = 15 minutes (used in config.py)
PG_TOKEN_REFRESH_SECONDS=900

# =============================================================================
# DATABRICKS APPS DEPLOYMENT
# =============================================================================
# Port for Databricks Apps deployment (used in config.py and server.py)
DATABRICKS_APP_PORT=8080

# =============================================================================
# CONFIGURATION VALIDATION
# =============================================================================
# REQUIRED VARIABLES:
# - DATABRICKS_HOST, DATABRICKS_TOKEN (for Databricks connection)
# - Either WAREHOUSE_ID or DATABRICKS_HTTP_PATH (for SQL warehouse)
# - PGDATABASE, PGUSER, PGHOST (for PostgreSQL/Lakebase connection)
#
# OPTIONAL WITH DEFAULTS:
# - All other variables have reasonable defaults if not specified
#
# SECURITY NOTE: Never commit actual credentials - keep them in .env (in .gitignore)
