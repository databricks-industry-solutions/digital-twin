# =============================================================================
# DATABRICKS DIGITAL TWIN - FRONTEND ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains ONLY the environment variables that are actually used in the frontend code
# Copy this file to .env.local and configure with your specific values

# =============================================================================
# BACKEND API CONFIGURATION (RECOMMENDED SECURE SETUP)
# =============================================================================
# The backend URL for API calls - this is the recommended secure setup where 
# the frontend only communicates with the backend, and the backend handles all Databricks authentication
REACT_APP_BACKEND_URL=http://localhost:8080

# =============================================================================
# DIRECT DATABRICKS CONFIGURATION (TESTING/DEVELOPMENT ONLY)
# =============================================================================
# ⚠️  WARNING: The following variables are for testing/development purposes only
# ⚠️  For production, use only REACT_APP_BACKEND_URL and let the backend handle Databricks authentication
# ⚠️  Never expose Databricks tokens in the frontend in production!

# Databricks workspace connection (used in telemetry service and testing scripts)
REACT_APP_DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
REACT_APP_DATABRICKS_TOKEN=dapixxxxxx
REACT_APP_WAREHOUSE_ID=your-warehouse-id
REACT_APP_DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your-warehouse-id

# Database schema configuration (used in telemetry service with defaults)
REACT_APP_DATABRICKS_CATALOG=main
REACT_APP_DATABRICKS_SCHEMA=your_schema
REACT_APP_DATABRICKS_TABLE=bronze

# Advanced table names (used in testing scripts)
REACT_APP_SYNCED_TABLE_FULL_NAME=main.your_schema.synced_table
REACT_APP_TRIPLE_TABLE_FULL_NAME=main.your_schema.latest_sensor_triples

# =============================================================================
# USAGE NOTES
# =============================================================================
# 1. SECURE PRODUCTION SETUP: Only set REACT_APP_BACKEND_URL
#    - The backend handles all Databricks authentication securely
#    - No Databricks credentials are exposed to the browser
#    
# 2. DIRECT DATABRICKS (TESTING ONLY): Set all Databricks variables
#    - Used by testing scripts in /frontend/testing/
#    - Used by telemetry service for direct Databricks connection
#    - NOT recommended for production due to security risks
#
# 3. All variables prefixed with REACT_APP_ are embedded in the build and visible to users
# 4. The telemetry service has fallback defaults for catalog (main), schema (default), table (bronze)
